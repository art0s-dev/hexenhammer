package unit.arch;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

class MVCTest {

	@Test
	void test() {
		//create the unit model
		//create the unit view 
		//create the controller
		/**
		model(view(controller(view, model)))
		
		user klickt view
		view startet request aus controller
		conroller manipuliert model
		model updated view
		
		
		view
		controller(view, model)
		model(view)
		
		
		new Controller(new Model(new View))*/
	}

}
