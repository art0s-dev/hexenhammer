package arch;


/**
 * Record for passing an Array
 * List of for example Units to the view
 * So its just a type used by the different Interfaces 
 * to connect the core with the architecture
 * for example if a new entity is added (vehicle)
 * we can extend a vehicleList that will be used in the vehicleView and controller 
 * to satisfy the interface.
 */
public abstract class ModelList {
}
